// src/components/game/PlayerHand.jsx
import React from 'react';


const PlayerHand = ({ cardData }) => {
    // For now, it just uses your main Card component.
    return (
        <p>PlayerHand placeholder</p> 
    );
};

export default PlayerHand